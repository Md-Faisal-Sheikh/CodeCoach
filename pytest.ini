[pytest]
testpaths = tests
# all tests are plain functions; this pattern matches none of our dataclasses
# (e.g. grader.TestSpec) so pytest does not try to collect them as test classes
python_classes = _NoTestClasses
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
